version: '3.8'

services:
  fialangchain:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fia-langchain-service
    ports:
      - "8086:8086"
    env_file:
      - .env
    environment:
      - PORT=8086
    restart: unless-stopped
